# 对话记录改进说明

## 概述
本次更新解决了对话记录管理的三个主要问题：
1. 对话记录未保存到咨询记录中
2. 用户离开会话时缺少提示
3. 人工客服界面缺少删除过时会话的功能

## 后端改进

### 1. 对话记录关联到Inquiry（`conversation.service.ts`）

#### 新增功能：`createInquiryFromSession(sessionId)`
- 自动从会话对话中提取用户信息（城市、学段、性别、身份、电话）
- 创建Inquiry记录并将所有对话关联到该记录
- 当用户提供完整信息（特别是电话号码）时自动触发

#### 新增功能：`handleUserLeaveSession(sessionId)`
- 用户离开会话时自动调用
- 创建系统消息："[系统消息] 用户已离开会话"
- 自动触发`createInquiryFromSession`保存对话记录

#### 新增功能：`archiveSession(sessionId)`
- 归档（删除）会话
- 在归档前确保对话记录已关联到Inquiry
- 删除Session记录但保留Conversation记录

### 2. WebSocket通知机制（`conversation.gateway.ts`）

#### 改进连接管理
- 新增`socketSessionMap`：追踪每个socket对应的会话和用户类型
- 在用户加入会话时记录用户类型（user/agent）

#### 用户离开通知
- `handleDisconnect`：检测用户断开连接
- 当用户（非客服）断开时，向该会话广播`user-left`事件
- `handleLeaveSession`：支持主动离开会话的通知

### 3. 新增API端点（`conversation.controller.ts`）

```typescript
POST /api/conversations/archive-session
Body: { sessionId: string }
功能：归档会话

POST /api/conversations/user-leave
Body: { sessionId: string }
功能：处理用户离开会话
```

## 前端改进

### 1. Socket通信增强（`socket.ts`）

#### 新增函数
```typescript
onUserLeft(callback)    // 监听用户离开事件
offUserLeft()          // 取消监听
```

### 2. API调用增强（`api.ts`）

#### 新增函数
```typescript
archiveSession(sessionId)  // 归档会话
```

### 3. 人工客服界面改进（`AgentChat.tsx`）

#### 新增功能
- **用户离开提示**
  - 顶部显示Alert警告："用户已离开会话"
  - 在消息列表中显示黄色系统消息
  - 弹出消息提示

- **会话列表删除功能**
  - 每个会话项添加删除按钮（红色垃圾桶图标）
  - 使用Popconfirm确认框防止误操作
  - 删除提示："确定要归档此会话吗？对话记录将保存到咨询记录中。"
  - 删除后自动刷新列表

- **系统消息显示**
  - 新增Message类型：`'user' | 'bot' | 'agent' | 'system'`
  - 系统消息居中显示，黄色背景，突出显示

#### 改进的监听机制
```typescript
onUserLeft((data) => {
  // 显示Alert
  setUserLeftAlert(true)
  // 显示消息提示
  antMessage.warning('用户已离开会话')
  // 在消息列表中添加系统消息
  setMessages(prev => [...prev, {
    id: `system-${Date.now()}`,
    sender: 'system',
    message: '用户已离开会话',
    createdAt: new Date().toISOString(),
  }])
})
```

### 4. 对话记录管理改进（`ConversationManagement.tsx`）

#### 新增统计卡片
- 咨询记录数
- 总消息数
- 用户消息数
- 客服消息数

#### 表格增强
- **用户信息列**：显示城市、学段、电话、身份、性别
- **对话数量列**：显示总数和分类统计（用户消息 vs 客服消息）
- **对话时间列**：显示对话开始和结束时间
- **状态列**：显示咨询状态（已联系/未联系）

#### 详情弹窗改进
- 顶部显示用户完整信息卡片
- 更大的弹窗尺寸（800px）
- 更高的消息列表（500px可滚动）
- 更清晰的消息分类和时间显示

#### 空状态优化
- 自定义Empty组件
- 友好的提示文字："当用户完成咨询或离开会话后，对话记录将自动保存至此"

## 工作流程

### 用户完成咨询流程
1. 用户与机器人/客服对话
2. 用户提供完整信息（特别是电话号码）
3. 用户关闭页面或断开连接
4. 后端触发`handleUserLeaveSession`
5. 创建Inquiry记录并关联所有对话
6. 客服收到"用户已离开会话"的通知

### 客服管理过时会话流程
1. 客服在会话列表看到多个会话
2. 对于已完成或过时的会话，点击删除按钮
3. 确认归档操作
4. 系统自动保存对话记录到Inquiry
5. 删除Session记录
6. 会话从列表中移除
7. 对话记录在"对话记录管理"中可查看

### 对话记录查看流程
1. 进入"对话记录管理"页面
2. 查看统计数据和列表
3. 点击"查看详情"查看完整对话
4. 查看用户信息和完整对话历史

## 技术亮点

1. **自动化数据保存**：用户离开时自动保存，无需手动操作
2. **实时通知**：WebSocket实时通知客服用户状态变化
3. **数据安全**：归档会话前确保数据已保存
4. **用户体验**：清晰的提示、确认机制、统计展示
5. **数据完整性**：Conversation通过inquiryId关联Inquiry，即使Session删除，对话记录仍保留

## 注意事项

1. **电话号码识别**：当前使用简单的正则匹配（`1[3-9]\d{9}`），可能需要根据实际情况调整
2. **城市信息提取**：当前匹配"XX市"格式，可以改进为更智能的地址解析
3. **会话清理**：建议定期清理长时间无活动的Session记录，但要确保对话已保存
4. **性能优化**：如果对话记录很多，考虑添加分页和搜索功能

## 测试建议

1. **用户离开场景测试**
   - 用户完成对话后关闭页面
   - 用户中途断开连接
   - 验证Inquiry是否创建
   - 验证对话是否关联

2. **客服删除场景测试**
   - 删除普通会话
   - 删除当前选中的会话
   - 验证确认对话框
   - 验证删除后数据保存

3. **对话记录展示测试**
   - 验证统计数据准确性
   - 验证列表显示
   - 验证详情弹窗
   - 验证空状态显示

## 后续优化建议

1. 添加对话记录导出功能（Excel/PDF）
2. 添加对话记录搜索和筛选功能
3. 改进信息提取算法（使用AI/NLP）
4. 添加会话标签和分类功能
5. 添加会话质量评分功能


