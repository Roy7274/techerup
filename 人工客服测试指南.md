# 人工客服功能 - 测试指南

## 🎯 功能已集成

人工客服管理界面已成功集成到管理后台！

## 📍 在哪里找到人工客服功能

### 管理后台入口

1. 访问管理后台：`http://localhost:3000/admin`
2. 登录管理员账号
3. 在左侧菜单中找到 **"人工客服"** 菜单项（带客服图标 👨‍💼）
4. 点击进入人工客服管理界面

## 🧪 完整测试流程

### 步骤 1：用户发起咨询

1. 在**新的浏览器标签页**打开前台页面：`http://localhost:3000`
2. 点击右下角的"在线咨询"按钮
3. 发送几条消息，观察 AI 自动回复
4. **点击"转人工"按钮** ⭐
5. 发送一条消息："我需要人工客服帮助"
6. **复制浏览器控制台中的 sessionId**（或从 URL/LocalStorage 获取）

> 💡 如何找到 sessionId：
> - 按 F12 打开浏览器开发者工具
> - 切换到 Console 标签
> - 输入：`localStorage.getItem('sessionId')` 或查看网络请求中的 sessionId

### 步骤 2：客服接入处理

1. 返回管理后台页面
2. 点击左侧菜单的 **"人工客服"**
3. 查看会话列表：
   - ✅ 如果看到会话，直接点击进入
   - ❌ 如果没有看到，点击右上角的 **"添加"** 按钮
   - 输入第一步复制的 sessionId
4. 查看完整的聊天历史（包括之前的 AI 对话）
5. 在输入框中输入回复："您好，我是人工客服，有什么可以帮您？"
6. 点击"发送"按钮

### 步骤 3：用户端验证

1. 返回前台聊天窗口
2. 等待几秒（或刷新页面）
3. 应该能看到：
   - ✅ 客服的回复消息
   - ✅ 客服图标（橙色）
   - ✅ 显示"人工客服"标签
   - ✅ 之前的聊天记录完整保留

### 步骤 4：验证会话持久化

1. **刷新前台页面**
2. 再次打开聊天窗口
3. 验证：
   - ✅ 所有历史消息都保留
   - ✅ 仍然显示为"人工客服"模式
   - ✅ 头部显示"人工客服"而不是"商家"
4. 发送新消息
5. 返回客服后台，应该能看到新消息

## 🎨 界面说明

### 人工客服后台界面

```
┌─────────────┬──────────────────────────────────┐
│ 会话列表    │  聊天区域                         │
│ (左侧)      │  (右侧)                           │
├─────────────┼──────────────────────────────────┤
│ 📊 统计信息 │  👤 会话: session_xxx...          │
│             │  ┌────────────────────────────┐  │
│ 🔄 刷新 ➕  │  │  用户: 你好                │  │
│             │  │  机器人: 您好！...          │  │
│ 会话 1      │  │  用户: 转人工              │  │
│ 📩 有新消息 │  │  人工客服: 已接入...       │  │
│             │  └────────────────────────────┘  │
│ 会话 2      │  ┌────────────────────────────┐  │
│             │  │ 输入消息...          [发送] │  │
│ 会话 3      │  └────────────────────────────┘  │
└─────────────┴──────────────────────────────────┘
```

### 功能特点

**左侧会话列表：**
- 显示所有待处理的会话
- 显示最后一条消息预览
- 显示最后活跃时间
- 显示未读消息数量（红色徽章）
- 自动每 30 秒刷新
- 手动刷新按钮
- 手动添加会话按钮

**右侧聊天区域：**
- 显示完整的聊天历史
- 区分用户/机器人/人工客服消息
- 实时发送和接收消息
- 自动滚动到最新消息
- 刷新消息按钮

## 🐛 常见问题排查

### 问题 1：会话列表为空

**原因：** 用户还没有点击"转人工"

**解决：**
1. 前台页面必须点击"转人工"按钮
2. 或者点击客服后台的"添加"按钮手动输入 sessionId
3. 点击"刷新列表"按钮

### 问题 2：找不到 sessionId

**方法 1 - 从控制台获取：**
```javascript
// 在前台页面按 F12，在 Console 中输入：
console.log('Session ID:', document.cookie)
// 或查看网络请求中的 sessionId 参数
```

**方法 2 - 从数据库获取：**
```sql
-- 查看所有会话
SELECT * FROM sessions ORDER BY created_at DESC LIMIT 10;

-- 查看已转人工的会话
SELECT * FROM sessions WHERE is_agent = true;
```

**方法 3 - 使用测试会话：**
```bash
# 创建一个测试会话
curl -X POST http://localhost:3001/api/conversations/message \
  -H "Content-Type: application/json" \
  -d '{"sessionId": "test_session_2024", "message": "测试消息"}'

# 转人工
curl -X POST http://localhost:3001/api/conversations/switch-agent \
  -H "Content-Type: application/json" \
  -d '{"sessionId": "test_session_2024"}'

# 然后在客服后台输入：test_session_2024
```

### 问题 3：客服回复后用户看不到

**可能原因：**
1. 前台页面没有刷新
2. 需要手动刷新消息

**解决：**
- 前台刷新页面
- 或实现 WebSocket 实时推送（推荐）

### 问题 4：历史记录没有保留

**检查：**
```sql
-- 查看会话的所有消息
SELECT * FROM conversations 
WHERE session_id = 'your_session_id' 
ORDER BY created_at ASC;

-- 检查会话状态
SELECT * FROM sessions WHERE session_id = 'your_session_id';
```

## 📊 数据库验证

### 查看所有会话
```sql
SELECT 
  s.session_id,
  s.is_agent,
  s.last_activity,
  COUNT(c.id) as message_count
FROM sessions s
LEFT JOIN conversations c ON c.session_id = s.session_id
GROUP BY s.id
ORDER BY s.last_activity DESC;
```

### 查看待处理会话
```sql
SELECT * FROM sessions 
WHERE is_agent = true 
ORDER BY last_activity DESC;
```

### 查看某个会话的完整对话
```sql
SELECT 
  sender,
  message,
  created_at
FROM conversations 
WHERE session_id = 'your_session_id' 
ORDER BY created_at ASC;
```

## 🎯 快速测试（使用 curl）

```bash
# 1. 创建测试会话
SESSION_ID="test_$(date +%s)"

# 2. 发送用户消息
curl -X POST http://localhost:3001/api/conversations/message \
  -H "Content-Type: application/json" \
  -d "{\"sessionId\": \"$SESSION_ID\", \"message\": \"你好，我需要咨询\"}"

# 3. 转人工
curl -X POST http://localhost:3001/api/conversations/switch-agent \
  -H "Content-Type: application/json" \
  -d "{\"sessionId\": \"$SESSION_ID\"}"

# 4. 查看会话
echo "Session ID: $SESSION_ID"
curl http://localhost:3001/api/conversations/session/$SESSION_ID

# 5. 在管理后台输入这个 SESSION_ID 即可看到该会话
```

## ✅ 验收标准

- [ ] 能在管理后台看到"人工客服"菜单
- [ ] 点击后显示会话列表和聊天界面
- [ ] 用户点击"转人工"后，会话出现在列表中
- [ ] 点击会话后能看到完整的聊天历史
- [ ] 客服可以发送消息
- [ ] 用户端能收到客服的回复
- [ ] 刷新页面后历史记录和状态都保留
- [ ] 会话列表显示未读消息数量
- [ ] 每 30 秒自动刷新会话列表

## 🚀 下一步优化建议

1. **实时通信** - 使用 WebSocket 实现实时消息推送
2. **桌面通知** - 新消息到达时显示浏览器通知
3. **音效提醒** - 新消息播放提示音
4. **快捷回复** - 预设常用回复模板
5. **会话备注** - 为会话添加标签和备注
6. **客服状态** - 显示客服在线/离线状态

---

祝测试顺利！如有问题请参考文档或检查浏览器控制台的错误信息。

