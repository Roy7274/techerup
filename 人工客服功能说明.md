# 人工客服介入功能说明

## 功能概述

实现了完整的人工客服介入聊天功能和聊天记录持久化存储。

## 主要改动

### 1. 数据库变更

新增 `Session` 表用于跟踪会话状态：

```prisma
model Session {
  id          String   @id @default(uuid())
  sessionId   String   @unique
  isAgent     Boolean  @default(false)  // 是否已转人工客服
  agentId     String?                   // 人工客服ID
  metadata    Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  lastActivity DateTime @default(now()) // 最后活跃时间
}
```

### 2. 后端功能

#### 新增服务方法 (conversation.service.ts)

- `getOrCreateSession()`: 获取或创建会话
- `updateSessionActivity()`: 更新会话活跃时间
- `isAgentSession()`: 检查会话是否已转人工
- `sendAgentMessage()`: 人工客服发送消息
- `getSessionStatus()`: 获取会话状态

#### 新增API端点 (conversation.controller.ts)

- `POST /conversations/agent-message`: 人工客服发送消息
- `GET /conversations/session-status/:sessionId`: 获取会话状态

#### 优化逻辑

- `handleUserMessage()`: 现在会检查会话状态，如果已转人工客服，则不自动回复
- `switchToAgent()`: 更新会话状态到数据库，并保存系统消息

### 3. 前端功能

#### 新增API接口 (api.ts)

- `sendAgentMessage()`: 调用人工客服发送消息接口
- `getSessionStatus()`: 获取会话状态

#### ChatWidget优化

- 初始化时检查会话状态，恢复人工客服模式
- 切换到人工客服后，正确保留所有历史聊天记录
- 消息发送后自动检查并更新会话状态
- 根据会话状态显示正确的发送者图标和名称

## 工作流程

### 用户侧流程

1. 用户发送消息 → 系统自动回复（AI模式）
2. 用户点击"转人工" → 会话状态变为人工客服模式
3. 人工客服接入消息自动显示
4. 之后用户消息不再自动回复，等待人工客服回复
5. 刷新页面后，会话状态保持，继续显示人工客服模式

### 人工客服侧流程

1. 查看待处理会话列表
2. 通过 `POST /conversations/agent-message` 发送消息
3. 消息自动标记为 `sender: 'agent'`
4. 用户侧实时接收人工客服消息

## 数据持久化

- 所有消息（用户/机器人/人工客服）都保存到 `Conversation` 表
- 会话状态保存到 `Session` 表
- 通过 `sessionId` 关联所有消息和会话状态
- 支持跨设备、跨浏览器会话恢复

## 使用说明

### 1. 运行数据库迁移

首次使用需要运行数据库迁移来创建 Session 表：

```bash
cd backend
npx prisma migrate dev
```

### 2. 启动后端服务

```bash
cd backend
npm run start:dev
```

### 3. 启动前端服务

```bash
cd frontend
npm run dev
```

### 4. 测试功能

#### 用户端测试：

1. 打开前台页面，点击在线咨询
2. 发送消息，观察AI自动回复
3. 点击"转人工"按钮
4. 继续发送消息（此时不会自动回复，等待客服）
5. 刷新页面，确认会话状态保持（仍显示人工客服模式）

#### 客服端测试：

1. 在管理后台集成 `AgentChat` 组件
2. 会话列表会自动显示需要处理的会话
3. 点击会话查看聊天记录
4. 发送消息给用户
5. 用户端会立即看到客服回复

### 5. 集成人工客服界面

在管理后台页面中集成客服组件：

```tsx
import AgentChat from '@/components/admin/AgentChat'

// 在你的管理后台页面中
export default function AdminPage() {
  return (
    <div>
      <h1>人工客服管理</h1>
      <AgentChat />
    </div>
  )
}
```

## API 接口文档

### 用户端 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/conversations/message` | POST | 发送用户消息 |
| `/api/conversations/session/:sessionId` | GET | 获取会话历史记录 |
| `/api/conversations/switch-agent` | POST | 切换到人工客服 |
| `/api/conversations/session-status/:sessionId` | GET | 获取会话状态 |

### 客服端 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/conversations/agent-message` | POST | 人工客服发送消息 |
| `/api/conversations/active-sessions` | GET | 获取所有活跃会话 |
| `/api/conversations/pending-agent-sessions` | GET | 获取待处理的会话 |

## 测试示例

### 1. 用户发送消息

```bash
curl -X POST http://localhost:3001/api/conversations/message \
  -H "Content-Type: application/json" \
  -d '{
    "sessionId": "test_session_123",
    "message": "你好"
  }'
```

### 2. 切换到人工客服

```bash
curl -X POST http://localhost:3001/api/conversations/switch-agent \
  -H "Content-Type: application/json" \
  -d '{
    "sessionId": "test_session_123"
  }'
```

### 3. 客服发送消息

```bash
curl -X POST http://localhost:3001/api/conversations/agent-message \
  -H "Content-Type: application/json" \
  -d '{
    "sessionId": "test_session_123",
    "message": "您好，我是客服，有什么可以帮您？",
    "agentId": "admin"
  }'
```

### 4. 查看会话记录

```bash
curl http://localhost:3001/api/conversations/session/test_session_123
```

## 数据库查询

### 查看所有会话

```sql
SELECT * FROM sessions ORDER BY last_activity DESC;
```

### 查看某个会话的所有消息

```sql
SELECT * FROM conversations 
WHERE session_id = 'test_session_123' 
ORDER BY created_at ASC;
```

### 查看已转人工的会话

```sql
SELECT * FROM sessions 
WHERE is_agent = true 
ORDER BY last_activity DESC;
```

## 已实现功能清单

- ✅ 会话状态管理（Session表）
- ✅ 聊天记录持久化存储
- ✅ 人工客服介入功能
- ✅ 切换人工后保留历史记录
- ✅ 会话状态跨页面保持
- ✅ 人工客服后台界面
- ✅ 活跃会话列表
- ✅ 待处理会话列表
- ✅ 未读消息统计
- ✅ 自动刷新会话列表（30秒）

## 下一步扩展

可以考虑实现以下功能：

1. ✅ 人工客服后台管理界面（已实现）
   - ✅ 查看所有活跃会话
   - ✅ 实时接收用户消息通知
   - ✅ 回复用户消息

2. 实时通信（推荐）
   - 使用 WebSocket 实现实时消息推送
   - 避免轮询，提升性能
   - 用户和客服都能实时看到新消息

3. 消息已读状态
   - 标记消息是否已读
   - 显示"对方正在输入..."

4. 会话分配
   - 自动分配会话给空闲客服
   - 客服工作负载均衡
   - 支持多客服并发处理

5. 服务质量统计
   - 平均响应时间
   - 客户满意度评分
   - 会话转化率
   - 客服工作量统计

6. 富文本消息
   - 支持图片、文件上传
   - 支持表情符号
   - 支持Markdown格式

