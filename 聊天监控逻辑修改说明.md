# 聊天人工介入逻辑修改说明

## 修改概述

根据需求，将聊天人工介入逻辑从"用户主动点击转人工按钮"改为"后台自动监控，客服随时介入"，同时保留自动回复机制。

## 主要修改内容

### 1. 前端修改 (ChatWidget.tsx)

#### 删除的功能：
- 移除了 `switchToAgent` API 的导入
- 删除了 `handleSwitchToAgent` 函数
- 移除了转人工按钮相关的UI逻辑

#### 保留的功能：
- 自动回复机制继续正常工作
- 会话状态管理（`isAgent` 状态）
- WebSocket 实时通信
- 消息历史记录

### 2. 后端修改 (conversation.service.ts)

#### 新增功能：
- **自动监控所有会话**：所有用户消息都会通知客服，无论是否已转人工
- **新会话通知**：会话创建时自动通知客服有新会话可以监控
- **增强的会话列表**：`getActiveSessions` 和 `getPendingAgentSessions` 现在返回所有活跃会话

#### 修改的逻辑：
```typescript
// 在 handleUserMessage 中
// 通知所有客服有新消息（无论是否已转人工，都让客服能监控到）
this.conversationGateway.notifyAgentsNewMessage({
  sessionId,
  type: 'new-user-message',
  message: userMessage,
  isAgent,
});
```

#### 新增的会话信息：
- `canIntervene`: 标记是否可以介入（未转人工的会话）
- `status`: 会话状态（'auto' 或 'agent'）

### 3. WebSocket 修改 (conversation.gateway.ts)

#### 新增方法：
```typescript
// 通知客服有新消息（包括未转人工的会话）
notifyAgentsNewMessage(data: any) {
  this.logger.log('通知所有客服有新消息');
  this.server.emit('new-user-message', data);
}
```

### 4. 前端 Socket 修改 (socket.ts)

#### 新增监听器：
```typescript
// 监听新用户消息（客服端监控所有会话）
export const onNewUserMessage = (callback: (data: any) => void) => {
  const socket = getSocket()
  if (socket) {
    socket.on('new-user-message', callback)
  }
}
```

## 新的工作流程

### 用户端流程：
1. 用户进入页面 → 自动创建会话
2. 用户发送消息 → 系统自动回复（AI模式）
3. 客服可以随时介入 → 发送消息后自动转为人工模式
4. 转为人工后 → 停止自动回复，等待客服回复

### 客服端流程：
1. 客服登录后台 → 自动接收所有活跃会话列表
2. 实时监控 → 收到所有用户消息通知（包括未转人工的）
3. 随时介入 → 客服可以回复任何会话，自动转为人工模式
4. 会话管理 → 查看所有活跃会话，包括自动回复和人工介入的

## 技术实现细节

### 会话状态管理：
- `isAgent: false` - 自动回复模式，客服可监控
- `isAgent: true` - 人工客服模式，停止自动回复

### 消息通知机制：
- 所有用户消息都会通过 WebSocket 通知客服
- 新会话创建时也会通知客服
- 客服可以实时看到所有活跃会话的状态

### 自动回复保留：
- 未转人工的会话继续使用原有的自动回复逻辑
- 转人工后停止自动回复，等待客服处理

## 优势

1. **主动监控**：客服可以主动发现需要帮助的用户
2. **无缝介入**：客服可以随时介入任何会话，无需用户操作
3. **保留自动化**：自动回复机制继续工作，减少客服工作量
4. **实时响应**：通过 WebSocket 实现实时消息推送
5. **完整记录**：所有会话和消息都有完整的历史记录

## 测试建议

### 用户端测试：
1. 打开聊天页面，发送消息，确认自动回复正常
2. 刷新页面，确认会话状态保持
3. 等待客服介入，确认转为人工模式

### 客服端测试：
1. 登录管理后台，查看活跃会话列表
2. 实时接收用户消息通知
3. 回复用户消息，确认自动转为人工模式
4. 验证会话状态正确更新

## 注意事项

1. 原有的 `switchToAgent` API 仍然保留，供客服手动转人工使用
2. 自动回复逻辑完全保留，不影响现有功能
3. WebSocket 连接需要确保稳定性
4. 客服端需要实现相应的界面来显示和操作会话列表

## 兼容性

- 前端：完全向后兼容，只是移除了转人工按钮
- 后端：API 接口保持不变，只是增强了监控功能
- 数据库：无需修改，使用现有的 Session 和 Conversation 表



