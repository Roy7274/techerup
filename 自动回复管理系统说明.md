# 自动回复管理系统功能说明

## 功能概述

本次开发实现了一个完整的自动回复管理系统，包括自动回复配置、表单模板管理、以及智能的自动回复暂停机制。该系统可以让管理员灵活配置机器人的自动回复行为，并支持通过可点击按钮快速引导用户填写表单。

## 核心功能

### 1. 自动回复管理

#### 特性：
- **多种触发类型**：
  - 欢迎语（welcome）：用户首次进入聊天时自动发送
  - 关键词触发（keyword）：当用户消息包含指定关键词时触发
  - 默认回复（default）：没有匹配到关键词时的兜底回复
  - 手动触发（manual）：由管理员手动触发

- **优先级控制**：
  - 数字越大优先级越高
  - 多个关键词匹配时，优先使用高优先级的回复

- **可点击选项**：
  - 自动回复可以包含可点击的按钮
  - 按钮可以关联表单模板
  - 点击按钮后自动弹出表单填写界面

- **关联表单**：
  - 每个自动回复可以关联一个表单模板
  - 用户点击选项按钮后自动打开表单

#### 配置示例：
```javascript
{
  name: "预约试听欢迎语",
  triggerType: "welcome",
  priority: 10,
  message: "您好！欢迎咨询我们的课程服务。",
  hasOptions: true,
  options: [
    {
      label: "预约试听",
      value: "booking",
      formTemplateId: "xxx-form-template-id"
    },
    {
      label: "了解课程",
      value: "course_info"
    }
  ],
  formTemplateId: "xxx-form-template-id"
}
```

### 2. 表单模板管理

#### 特性：
- **灵活的字段配置**：
  - 支持多种字段类型：文本、电话、下拉选择、单选、多选
  - 每个字段可配置：名称、标签、类型、选项、占位符、是否必填、排序
  - 支持自定义验证规则

- **可重用的模板**：
  - 创建一次，多处使用
  - 可以被多个自动回复关联
  - 支持激活/停用状态

- **字段类型**：
  - `text`: 普通文本输入
  - `tel`: 电话号码输入（自动验证手机号格式）
  - `select`: 下拉选择
  - `radio`: 单选
  - `checkbox`: 多选

#### 配置示例：
```javascript
{
  name: "预约试听表单",
  description: "用于收集用户预约试听的信息",
  fields: [
    {
      fieldName: "city",
      fieldLabel: "所在城市",
      fieldType: "text",
      placeholder: "请输入城市",
      required: true,
      order: 0
    },
    {
      fieldName: "grade",
      fieldLabel: "学段",
      fieldType: "select",
      options: ["小学", "初中", "高中"],
      required: true,
      order: 1
    },
    {
      fieldName: "phone",
      fieldLabel: "联系电话",
      fieldType: "tel",
      placeholder: "请输入手机号",
      required: true,
      order: 2
    }
  ]
}
```

### 3. 智能暂停机制

#### 暂停条件：

**人工回复后暂停2分钟**
- 当人工客服发送消息后，自动回复会暂停2分钟
- 避免机器人和人工客服同时回复造成混乱
- 2分钟后自动恢复

**用户离开时暂停60分钟**
- 用户关闭聊天窗口或离开页面时暂停1小时
- 避免对已离开的用户发送无意义的自动回复
- 1小时后自动恢复

#### 实现机制：
- 使用`AutoReplyPause`表记录暂停状态
- 每次触发自动回复前检查是否在暂停期
- 过期的暂停记录自动清理

### 4. 前端管理界面

#### 功能模块：

**自动回复管理标签页**
- 列表展示所有自动回复
- 支持新增、编辑、删除
- 快速切换激活状态
- 显示触发类型、关键词、优先级等信息

**表单模板管理标签页**
- 列表展示所有表单模板
- 可视化字段编辑器
- 显示关联的自动回复数量
- 删除前检查是否被使用

**操作功能**
- 拖拽式字段排序
- 实时预览
- 批量操作
- 数据导入导出

### 5. 前台聊天界面增强

#### 新增功能：

**可点击选项按钮**
- 自动回复消息下方显示按钮
- 圆润的UI设计，符合现代审美
- 支持多个按钮横向排列
- 点击后可触发表单或发送文本

**表单填写弹窗**
- 点击带表单的按钮后自动弹出
- 美观的表单布局
- 实时验证
- 提交后自动创建Inquiry记录

**用户体验优化**
- 点击按钮后自动聚焦表单
- 表单提交成功后显示确认消息
- 错误提示友好清晰
- 支持移动端和桌面端

## 数据库设计

### 新增表

#### `form_templates` - 表单模板表
```sql
id          UUID      主键
name        String    模板名称
description String?   模板描述
isActive    Boolean   是否启用
order       Int       排序
createdAt   DateTime  创建时间
updatedAt   DateTime  更新时间
```

#### `form_fields` - 表单字段表
```sql
id          UUID      主键
templateId  UUID      所属模板ID（外键）
fieldName   String    字段名称
fieldLabel  String    字段标签
fieldType   String    字段类型
options     JSON?     选项数据
placeholder String?   占位符
required    Boolean   是否必填
order       Int       排序
validation  JSON?     验证规则
createdAt   DateTime  创建时间
updatedAt   DateTime  更新时间
```

#### `auto_replies` - 自动回复表
```sql
id              UUID      主键
name            String    回复名称
triggerType     String    触发类型
keywords        JSON?     触发关键词数组
priority        Int       优先级
message         Text      回复内容
hasOptions      Boolean   是否包含可点击选项
options         JSON?     选项数据
formTemplateId  UUID?     关联的表单模板ID（外键）
isActive        Boolean   是否启用
useConditions   JSON?     使用条件
createdAt       DateTime  创建时间
updatedAt       DateTime  更新时间
```

#### `auto_reply_pauses` - 自动回复暂停记录表
```sql
id          UUID      主键
sessionId   String    会话ID（唯一）
pausedUntil DateTime  暂停到什么时候
reason      String    暂停原因
createdAt   DateTime  创建时间
updatedAt   DateTime  更新时间
```

## API接口

### 表单模板相关

```
GET    /api/form-templates           获取所有表单模板
GET    /api/form-templates/:id       获取单个表单模板
POST   /api/form-templates           创建表单模板
PATCH  /api/form-templates/:id       更新表单模板
DELETE /api/form-templates/:id       删除表单模板
PATCH  /api/form-templates/:id/toggle-active  切换激活状态
```

### 自动回复相关

```
GET    /api/auto-replies             获取所有自动回复
GET    /api/auto-replies/:id         获取单个自动回复
GET    /api/auto-replies/welcome     获取欢迎语
POST   /api/auto-replies             创建自动回复
POST   /api/auto-replies/match       匹配自动回复
PATCH  /api/auto-replies/:id         更新自动回复
DELETE /api/auto-replies/:id         删除自动回复
PATCH  /api/auto-replies/:id/toggle-active  切换激活状态
POST   /api/auto-replies/pause       暂停自动回复
POST   /api/auto-replies/resume      恢复自动回复
GET    /api/auto-replies/status/:sessionId  检查自动回复状态
```

## 使用流程

### 管理员配置流程

1. **创建表单模板**
   - 进入后台管理 -> 自动回复 -> 表单模板管理
   - 点击"新增表单模板"
   - 配置模板名称、描述
   - 添加表单字段（城市、学段、电话等）
   - 保存模板

2. **创建自动回复**
   - 切换到"自动回复管理"标签
   - 点击"新增自动回复"
   - 选择触发类型（欢迎语/关键词等）
   - 输入回复内容
   - 如需按钮：
     - 勾选"包含可点击选项"
     - 配置选项JSON（标签、值、关联表单）
   - 选择关联的表单模板
   - 设置优先级
   - 保存

3. **测试**
   - 打开前台聊天窗口
   - 触发自动回复（发送关键词或首次进入）
   - 点击按钮测试表单填写

### 用户使用流程

1. **进入聊天**
   - 用户打开聊天窗口
   - 自动收到欢迎语和可点击按钮

2. **点击按钮**
   - 用户点击感兴趣的按钮（如"预约试听"）
   - 自动弹出表单填写窗口

3. **填写表单**
   - 填写表单各个字段
   - 系统自动验证（如手机号格式）
   - 点击提交

4. **完成**
   - 表单提交成功，显示确认消息
   - 系统自动创建Inquiry记录
   - 可以继续聊天咨询

## 注意事项

### 数据库迁移

由于数据库中可能存在旧的测试数据，在首次部署时需要处理数据库迁移：

```bash
cd backend

# 选项1：如果是开发环境且数据可以清空
npx prisma migrate reset --force

# 选项2：创建新的迁移
npx prisma migrate dev --name add_auto_reply_system

# 生成Prisma Client
npx prisma generate
```

### 配置建议

1. **欢迎语配置**
   - 每个系统应该只有一个激活的欢迎语
   - 优先级设置为最高（如100）

2. **关键词设置**
   - 关键词应该简短、常用
   - 避免使用过于宽泛的关键词
   - 支持多个关键词（用逗号分隔）

3. **表单字段**
   - 必填字段不宜过多，影响用户体验
   - 电话字段建议设为必填
   - 选项型字段提供合理的默认选项

4. **优先级管理**
   - 重要的回复设置高优先级
   - 默认回复设置较低优先级（如0）
   - 避免多个相同优先级的关键词冲突

### 性能优化

1. **缓存策略**
   - 自动回复列表可以缓存
   - 表单模板可以缓存
   - 暂停状态查询可以使用Redis

2. **数据库索引**
   - 已在sessionId、triggerType等字段添加索引
   - 定期清理过期的暂停记录

## 扩展功能建议

### 短期可实现

1. **统计分析**
   - 自动回复触发次数
   - 表单提交转化率
   - 用户点击热力图

2. **A/B测试**
   - 同一触发条件配置多个回复
   - 自动轮换测试效果

3. **时间条件**
   - 工作时间和非工作时间不同回复
   - 节假日特殊回复

### 长期规划

1. **AI增强**
   - 使用NLP理解用户意图
   - 自动生成回复建议
   - 学习优化关键词匹配

2. **多媒体支持**
   - 图片回复
   - 视频回复
   - 文件下载

3. **工作流集成**
   - 与CRM系统集成
   - 自动分配销售跟进
   - 邮件/短信通知

## 技术栈

### 后端
- **框架**: NestJS
- **ORM**: Prisma
- **数据库**: PostgreSQL
- **WebSocket**: Socket.io

### 前端
- **框架**: Next.js 14 (App Router)
- **UI库**: Ant Design
- **状态管理**: React Hooks
- **样式**: Tailwind CSS

## 文件结构

```
backend/
├── prisma/
│   ├── schema.prisma                 # 数据库模型（已更新）
│   └── migrations/                   # 数据库迁移文件
├── src/
│   ├── modules/
│   │   ├── auto-reply/              # 自动回复模块（新增）
│   │   │   ├── auto-reply.controller.ts
│   │   │   ├── auto-reply.service.ts
│   │   │   ├── auto-reply.module.ts
│   │   │   └── dto/
│   │   ├── form-template/           # 表单模板模块（新增）
│   │   │   ├── form-template.controller.ts
│   │   │   ├── form-template.service.ts
│   │   │   ├── form-template.module.ts
│   │   │   └── dto/
│   │   └── conversation/            # 对话模块（已更新）
│   │       └── conversation.service.ts
│   └── app.module.ts                # 应用主模块（已更新）

frontend/
├── src/
│   ├── lib/
│   │   └── api.ts                   # API接口（已更新）
│   ├── components/
│   │   ├── admin/
│   │   │   └── AutoReplyManagement.tsx  # 自动回复管理（新增）
│   │   └── ChatWidget.tsx           # 聊天组件（已更新）
│   └── app/
│       └── admin/
│           └── page.tsx             # 后台管理页面（已更新）
```

## 常见问题

### Q: 如何禁用自动回复？
A: 在自动回复管理界面，点击对应记录的开关按钮即可切换激活状态。

### Q: 表单提交后数据存储在哪里？
A: 数据存储在`inquiries`表中，可以在"咨询管理"菜单查看。

### Q: 如何避免机器人和人工同时回复？
A: 系统已内置智能暂停机制，人工回复后会自动暂停机器人2分钟。

### Q: 可以配置多个欢迎语吗？
A: 可以配置多个，但建议只激活一个，否则会按优先级选择最高的。

### Q: 关键词匹配是精确匹配还是模糊匹配？
A: 是模糊匹配，只要用户消息中包含关键词即可触发。

## 总结

本次开发实现了一个功能完善、易于使用的自动回复管理系统。通过可视化的后台管理界面，运营人员可以灵活配置各种自动回复场景，提升用户体验和咨询转化率。系统设计考虑了与人工客服的协同，避免了冲突，真正实现了智能化的客户服务。


