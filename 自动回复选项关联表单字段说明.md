# 自动回复选项关联表单字段功能说明

## 🎯 核心改进

新的逻辑更加智能和人性化：

### 改进前的问题
- ❌ "需要填表"复选框逻辑不清晰
- ❌ 用户点击选项后还要打开表单填写
- ❌ 无法通过对话逐步收集信息

### 改进后的优势
- ✅ 用户点击选项 = 自动发送消息
- ✅ 选项可以关联表单字段，自动保存
- ✅ 支持对话式收集信息
- ✅ 信息不完整也会保存

## 📋 两种表单提交方式

### 方式1：完整表单提交（必填所有字段）
用户主动点击"预约试听"按钮 → 弹出表单 → 填写所有字段 → 提交

**特点：**
- 必须填写所有必填字段
- 表单验证严格
- 适合快速预约的用户

### 方式2：对话式收集（信息不全也保存）
通过对话逐步收集 → 点击选项按钮 → 自动保存到表单 → 后台能看到部分信息

**特点：**
- 不要求填写所有字段
- 有多少保存多少
- 适合渐进式咨询的用户
- 降低用户填表压力

## 🎨 配置示例

### 示例1：询问学段

**配置自动回复：**
```
回复名称：询问学段
触发类型：手动触发（或关键词）
回复内容：请问您孩子目前是什么学段呢？

可点击选项：
┌─────────────────────────────────────┐
│ 按钮文字：小学                        │
│ 保存到：学段                          │
│ 保存值：小学                          │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 按钮文字：初中                        │
│ 保存到：学段                          │
│ 保存值：初中                          │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 按钮文字：高中                        │
│ 保存到：学段                          │
│ 保存值：高中                          │
└─────────────────────────────────────┘

关联表单收集：预约试听表单
```

**用户体验：**
1. 机器人：请问您孩子目前是什么学段呢？
2. 显示三个按钮：[小学] [初中] [高中]
3. 用户点击"小学"
4. 自动发送消息："小学"
5. 系统保存：`grade = "小学"`
6. 机器人继续下一个问题

### 示例2：询问性别

**配置自动回复：**
```
回复名称：询问性别
回复内容：请问您的孩子是男孩还是女孩呢？

可点击选项：
┌─────────────────────────────────────┐
│ 按钮文字：男孩                        │
│ 保存到：学生性别                      │
│ 保存值：男孩                          │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 按钮文字：女孩                        │
│ 保存到：学生性别                      │
│ 保存值：女孩                          │
└─────────────────────────────────────┘

关联表单收集：预约试听表单
```

### 示例3：欢迎语（不关联字段）

**配置自动回复：**
```
回复名称：欢迎语
触发类型：欢迎语
回复内容：您好！欢迎咨询我们的1对1家教服务。

可点击选项：
┌─────────────────────────────────────┐
│ 按钮文字：预约试听                    │
│ 保存到：（不选择）                    │
│ 保存值：（留空）                      │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 按钮文字：了解课程                    │
│ 保存到：（不选择）                    │
│ 保存值：（留空）                      │
└─────────────────────────────────────┘

关联表单收集：预约试听表单
```

## 🔄 完整对话流程示例

### 场景：用户通过对话逐步提供信息

```
用户：[打开聊天]

机器人：您好！欢迎咨询我们的1对1家教服务。
       [预约试听] [了解课程]

用户：[点击"了解课程"]
💾 系统：不保存（因为这个选项没有关联字段）

机器人：我们提供小学到高中各个学段的1对1辅导...请问您孩子是什么学段呢？
       [小学] [初中] [高中]

用户：[点击"初中"]
💾 系统：保存 grade = "初中"

机器人：好的，初中阶段的课程。请问您的孩子是男孩还是女孩？
       [男孩] [女孩]

用户：[点击"男孩"]
💾 系统：保存 studentGender = "男孩"

机器人：了解了。请问您是孩子本人还是家长呢？
       [本人] [家长]

用户：[点击"家长"]
💾 系统：保存 identity = "家长"

机器人：好的。您方便留个联系电话吗？我们老师会尽快与您联系。

用户：13812345678
💾 系统：保存 phone = "13812345678"

机器人：感谢您的信息！我们会尽快与您联系安排试听。

[此时系统创建Inquiry记录，包含：]
- city: 未知（用户没提供）
- grade: 初中
- studentGender: 男孩
- identity: 家长
- phone: 13812345678
- status: 未联系
```

## 🎛️ 字段说明

### 按钮文字
- 用户看到的按钮文本
- 点击后会发送到聊天
- 示例：`男孩`、`女孩`、`预约试听`

### 保存到
- 选择要保存到哪个表单字段
- 可选项：
  - 不选择（不保存）
  - 所在城市 (city)
  - 学段 (grade)
  - 学生性别 (studentGender)
  - 咨询身份 (identity)
- 注意：联系电话不在这里配置（用户直接输入）

### 保存值
- 实际保存到数据库的值
- 默认使用"按钮文字"
- 可以自定义（如果按钮文字和保存值不同）
- 示例：
  - 按钮文字：`男孩子` → 保存值：`男孩`
  - 按钮文字：`我是家长` → 保存值：`家长`

### 关联表单收集
- 选择是否要收集信息到表单
- 如果选择了表单模板，用户的选项会保存
- 如果不选择，只是普通的快捷回复

## 📊 后台查看

### 咨询管理界面

管理员可以看到两种情况：

**情况1：完整信息**
```
姓名：张三
城市：上海
学段：初中
性别：男孩
身份：家长
电话：13812345678
状态：未联系
```

**情况2：部分信息**
```
姓名：-
城市：未知
学段：初中
性别：男孩
身份：未知
电话：会话-abc12345
状态：信息不完整
```

> 注意：即使信息不完整，系统也会保存，方便后续跟进

## 🎯 最佳实践

### 1. 设计对话流程

建议的对话顺序：
1. 欢迎语（提供快捷选项）
2. 询问学段（小学/初中/高中）
3. 询问性别（男孩/女孩）
4. 询问身份（本人/家长）
5. 请求电话号码

### 2. 选项关联原则

**需要关联字段的选项：**
- 有明确答案的问题（学段、性别、身份）
- 需要保存到数据库的信息
- 用于后续统计分析的数据

**不需要关联字段的选项：**
- 导航类按钮（了解课程、价格咨询）
- 触发其他对话的按钮
- 纯展示性的选项

### 3. 按钮文字建议

- ✅ 简短清晰：`小学`、`初中`、`高中`
- ✅ 口语化：`男孩`、`女孩`
- ❌ 避免太长：`请选择小学阶段的课程`
- ❌ 避免技术术语

### 4. 保存值建议

- 大部分情况使用默认值（按钮文字）
- 只在需要规范化时自定义
- 保持与数据库约定一致

## 🔧 技术实现

### 数据保存机制

1. **用户点击选项**
   ```javascript
   {
     label: "男孩",
     fieldName: "studentGender",
     fieldValue: "男孩"
   }
   ```

2. **发送到后端**
   ```javascript
   metadata: {
     fromOption: true,
     fieldName: "studentGender",
     fieldValue: "男孩"
   }
   ```

3. **保存到Session**
   ```javascript
   session.metadata.formData = {
     studentGender: "男孩"
   }
   ```

4. **创建Inquiry记录**
   - 优先使用formData中的数据
   - 其次从对话内容中提取
   - 缺失的字段标记为"未知"

### 数据流转

```
用户点击选项
    ↓
发送消息（包含字段信息）
    ↓
保存到Session.metadata.formData
    ↓
机器人自动回复
    ↓
用户离开或点击预约
    ↓
创建Inquiry记录
    ↓
管理员在后台查看
```

## 📝 配置步骤

### Step 1: 创建自动回复

1. 登录后台管理
2. 点击"自动回复"菜单
3. 点击"新增自动回复"

### Step 2: 基本配置

```
回复名称：询问学段
触发类型：手动触发
回复内容：请问您孩子目前是什么学段呢？
```

### Step 3: 添加选项

1. 勾选"包含可点击选项"
2. 点击"添加选项"
3. 配置第一个选项：
   - 按钮文字：`小学`
   - 保存到：选择"学段"
   - 保存值：`小学`（或留空使用默认）

### Step 4: 关联表单

1. 在"关联表单收集"中选择"预约试听表单"
2. 点击"保存"

### Step 5: 测试

1. 打开前台聊天
2. 触发自动回复
3. 点击选项按钮
4. 查看后台咨询记录

## 🎓 常见问题

### Q1: 如果用户直接输入文字而不点击按钮怎么办？
A: 系统会保存用户输入的内容，并尝试从文字中提取信息。

### Q2: 如果用户点击了多次不同的选项会怎样？
A: 最后一次点击的值会覆盖之前的值。

### Q3: 电话号码可以通过按钮配置吗？
A: 不建议。电话号码应该让用户直接输入，系统会自动识别和验证。

### Q4: 可以一个自动回复配置多个字段保存吗？
A: 可以，每个选项可以关联不同的字段。比如一个问题问"您是家长吗？想了解哪个学段？"可以有多个选项关联不同字段。

### Q5: 不关联表单会怎样？
A: 选项只会发送消息，不会保存任何数据。适合纯导航型的按钮。

## 🚀 高级应用

### 多步骤表单收集

创建一系列相关的自动回复，形成完整的表单收集流程：

1. **欢迎语** → 触发了解意向
2. **询问学段** → 保存grade字段
3. **询问性别** → 保存studentGender字段
4. **询问身份** → 保存identity字段
5. **请求联系方式** → 让用户直接输入

每个步骤都是独立的自动回复，但通过关联同一个表单模板，最终收集完整信息。

---

**更新日期**：2025年10月20日
**版本**：v3.0 - 智能关联版


